const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const contact = await prisma.contact.create({ data: { addressLine1: '123 High Street', addressLine2: 'Flat 4', city: 'London', county: 'Greater London', postcode: 'SW1A 1AA', phoneNumber: '+44 20 1234 5678' } }); console.log('Created:', contact); } main().catch(console.error).finally(() => prisma.());
